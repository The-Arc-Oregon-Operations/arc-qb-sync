---
date: 2026-04-25
start_time: "16:00"
end_time: "TBD"
mode: code
participant: Alan Lytle
version_shipped: 3.7.0
orchestration_prompt: CLAUDE_CODE_PROMPT_v3.7.0.md
---

## Goal

Upgrade arc-qb-sync's image handling from URL-resolution-only to the Option A pipeline model, matching the pattern already in use by summit-qb-sync.

## What Was Done

**Files changed:**

- `arc-qb-sync/arc-qb-sync.php` — version bumped from 3.6.2 → 3.7.0 (header + constant)
- `arc-qb-sync/includes/qb-api.php` — replaced `arc_qb_sync_set_featured_image()` with Option A version (array $args signature, fast path, review gate, QB file download, URL fallback, writeback); added `arc_qb_upsert_record()`, `arc_qb_download_image_file()`, `arc_qb_write_image_attachment_id()`
- `arc-qb-sync/includes/sync-courses.php` — added shared `ARC_QB_IA_FID_*` constants (defined once, guarded); added Courses-specific pipeline FID constants (FK, Attachment ID, Review Status for featured + hero); added pipeline FIDs to both `$select` arrays (0-skip guarded); replaced featured image call with Option A; added hero writeback block
- `arc-qb-sync/includes/sync-events.php` — added Events-specific pipeline FID constants; added pipeline FIDs to select array; added `_arc_event_slug` meta storage; replaced featured image call with Option A; added hero writeback block
- `arc-qb-sync/includes/sync-instructors.php` — added headshot pipeline FID constants; added pipeline FIDs to select array; replaced headshot call with Option A
- `CHANGELOG.md` — added [3.7.0] entry dated 2026-04-25
- `CLAUDE_CODE_PROMPT_v3.7.0.md` — flipped status to `shipped`; added `shipped:` and `shipped_version:`
- `sessions/2026-04-25-1600_code_v3.7.0.md` — this file

## Branch / Commit

Branch: `claude/vigorous-shamir-f03443`
Commit: TBD (pending push)
Base commit before session: `5e93786`

## Open Questions / Notes

- `_arc_event_slug` was not previously stored in sync-events.php; added storage via `get_post_field( 'post_name', $post_id )` to provide a clean filename hint for hero image sideloads.
- All new FID constants are confirmed non-zero (from snapshot-2026-04-26-2048.md), so the 0-skip guards are a safety belt for future deployments, not active gates.
- wp-config.php on the production server must define `QB_IMAGE_ASSETS_TABLE_ID = 'bvx88yiv2'` before Option A activates.

## Next Session Handoff

Post-merge: run manual tests from the prompt's Section 8 table. Verify fast path on second sync, slow-path on first sync with Approved image, and review-gate skip on Unreviewed.
