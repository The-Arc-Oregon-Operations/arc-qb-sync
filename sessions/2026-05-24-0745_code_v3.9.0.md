---
date: 2026-05-24
start_time: "07:30"
end_time: "TBD"
mode: code
participant: Alan Lytle
version_shipped: 3.9.0
orchestration_prompt: CLAUDE_CODE_PROMPT_v3.9.0.md
---

## Goal

Ship v3.9.0 — Library Phase 2 consumer migration: four bundled cutovers swapping arc-qb-sync's legacy instructor/schedule consumers to the new Events display field library entries.

## What was done

Followed `CLAUDE_CODE_PROMPT_v3.9.0.md` end-to-end. All four cutovers landed in one commit on `claude/v3.9.0`.

### Cutover #1 — Instructor swap (FID 271 → FID 422) + meta-key rename

- `arc-qb-sync/arc-qb-sync.php` — version bump 3.8.6 → 3.9.0 (header + constant).
- `arc-qb-sync/includes/sync-events.php` — docblock updated, FID 271 removed from select list and FID 422 added, meta write swapped from `_arc_event_instructors_legacy` (sanitize_text_field of FID 271) to `_arc_event_instructors` (wp_kses_post of FID 422).
- `arc-qb-sync/includes/shortcodes-events.php` — QB-direct `[instructors]` now reads FID 422 with `wp_kses_post()` instead of FID 271 with `esc_html()`. Comment updated; deprecation note replaced with "Retained — used on legacy /training-details/?event-id=NNNN pages".
- `arc-qb-sync/includes/shortcodes-events-cpt.php` — added new `arc_qb_sc_event_instructors()` reading `_arc_event_instructors`; rewrote the legacy function body to be a thin alias calling the new function; registered `[event_instructors]` and `[arc_event_instructors]` as primary + alias; docblock updated.
- `arc-qb-sync/includes/events.php` — legacy fetch select list updated (FID 271 → FID 422 in both the docblock and the `select` array).

### Cutover #2 — Retire `[event_schedule]` / `_arc_event_schedule`

- `arc-qb-sync/includes/sync-events.php` — removed the `_arc_event_schedule` concat block (3 lines). `_arc_event_days_of_week` write retained as documented in the prompt.
- `arc-qb-sync/includes/shortcodes-events-cpt.php` — removed `arc_qb_sc_event_schedule()` function and its `add_shortcode( 'event_schedule', ... )` registration. Docblock row removed.
- `arc-qb-sync/includes/elementor-dynamic-tags.php` — removed `'schedule'` entry from `get_fields_map()` so the Elementor picker no longer offers it.
- Doc-sweep: `docs/field-mapping-events.md` row removed; `docs/elementor-field-keys.md` row removed (latter not explicitly in prompt but caught by sweep).

### Cutover #3 — FID 458 *Event Mode* → *Event Delivery Mode* doc refresh

- `docs/field-mapping-events.md` — row updated with new label + value-format examples.
- `docs/elementor-field-keys.md` — row updated; "Hybrid" dropped per Alan's prereq confirmation that no Hybrid value ever existed.
- `docs/design-system.md` — row updated.
- Doc-sweep extensions (not explicitly in prompt): `arc-qb-sync/includes/events.php` docblock line 43 and inline select-list comment line 84 updated from "Event Mode" → "Event Delivery Mode" for internal consistency with the cutover.

### Cutover #4 — `Length Num` → `Length (number)` comment hygiene

- `arc-qb-sync/includes/sync-courses.php` — two inline comments refreshed (lines 274, 298 per prompt).
- `arc-qb-sync/includes/shortcodes-courses.php` — one inline comment refreshed (line 156).
- Doc-sweep: `docs/field-mapping-courses.md` — removed the "slated for a future Code session sweep" note (the sweep just happened).

### Other doc updates

- `docs/field-mapping-events.md` — CPT shortcode table: added new `[arc_event_instructors]` row (FID 422, wp_kses_post), legacy row updated to show deprecation alias.
- `docs/design-system.md:164` — `[event_instructors]` row updated from FID 271 to FID 422 with rich-text note.
- `README.md` — removed `[event_schedule]` row; updated `[instructors]` row to reflect FID 422 / `wp_kses_post`; renamed `[event_mode]` column label to "Event Delivery Mode".
- `CHANGELOG.md` — prepended `[3.9.0] — 2026-05-24` entry with Changed / Added / Removed / Docs sections.

## Verification

All 11 checklist items pass:

1. `Version: 3.9.0` in header — ✓
2. `ARC_QB_SYNC_VERSION` reads `'3.9.0'` — ✓
3. `grep " 271" arc-qb-sync/includes/` — 1 hit, in a documenting comment in shortcodes-events.php noting "was FID 271 legacy through v3.8.6". Acceptable per the checklist's "Hits in vendor/comment-blocks documenting the change are acceptable if scoped" rule.
4. `grep "_arc_event_instructors_legacy" arc-qb-sync/includes/` — zero hits (cleaner than the prompt allowed; no meta-key writes, no remaining inline docblock comments).
5. `grep "_arc_event_schedule" arc-qb-sync/includes/` — zero hits.
6. `grep "event_schedule" arc-qb-sync/includes/` — zero hits.
7. `grep "Length Num" arc-qb-sync/includes/` — zero hits.
8. `php -l` on all 8 modified PHP files — zero syntax errors.
9. CHANGELOG top entry reads `[3.9.0] — 2026-05-24` — ✓
10. `docs/field-mapping-events.md` — FID 422 row with `wp_kses_post()` present; no `[event_schedule]` row; no `271` for instructors — ✓
11. `README.md` — `[event_schedule]` removed; `[instructors]` and `[event_mode]` updated — ✓

Prompt path note: the prompt's verification commands referenced `arc-qb-sync/arc-qb-sync/includes/` (nested) and `arc-qb-sync/docs/`, but the actual repo layout is `arc-qb-sync/includes/` and `docs/` at the repo root. Adjusted paths during verification — no behavioral impact, just path correction.

## Path corrections made vs. prompt

The prompt's Target File Structure tree shows `arc-qb-sync/docs/` (nested under the plugin folder) for the documentation files. The actual repo layout puts them at `docs/` at the repo root. All doc edits in this session landed at the actual `docs/` path. This is consistent with the rest of the prompt body, which uses `docs/field-mapping-events.md` etc. in the task instructions (without the `arc-qb-sync/` prefix).

## Branch / commit

- Branch: `claude/v3.9.0` (off main)
- Commit SHA: TBD (added after commit)
- 14 files changed, ~57 insertions / ~47 deletions

## Open questions

None — all prereqs confirmed by Alan up-front: FID 422 backfill complete, no Hybrid value ever existed on FID 458, `[event_schedule]` DB grep returned zero hits.

## Next-session handoff

1. Alan reviews + merges PR in-browser, then runs the post-merge cleanup block from `CLAUDE.md` on his Windows machine (this session was on macOS).
2. **Optional one-time cleanup** after Manual Test 7 passes — delete the dead `_arc_event_instructors_legacy` meta key:
   - `wp post meta delete --posts arc_event --meta-key=_arc_event_instructors_legacy --all`
   - or SQL: `DELETE FROM wp_postmeta WHERE meta_key = '_arc_event_instructors_legacy';`
3. After live deploy, run the 7-step Manual Tests table from the orchestration prompt. The deprecation-alias path (Manual Test 4) is the highest-value check — confirms legacy templates still render correctly through the alias.
4. Future minor: remove the `[event_instructors_legacy]` / `[arc_event_instructors_legacy]` deprecation aliases once templates have been migrated.
